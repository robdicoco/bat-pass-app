import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    fontSize: 20,
    alignItems: 'center',
  },
  container: {
    alignItems: 'center',
    width: '100%',
    height: 50,
    // borderColor: 'gray',
    borderWidth: 1,
    borderRadius: 8,
    // padding: 10,
    backgroundColor:'#E5BF3C',
  }
});